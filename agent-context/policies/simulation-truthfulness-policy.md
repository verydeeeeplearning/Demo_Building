# Simulation Truthfulness Policy

Behavior simulation shows what the circuit should appear to do. Electrical simulation explains whether simplified current paths are valid for supported models.

Do not claim SPICE accuracy. Current estimates are approximate. Modules may be modeled as black boxes with registry current values.

Current animation is allowed only for validated closed paths. Fault visualization must clearly show missing ground, over-current, short circuit, or unsupported model states.
