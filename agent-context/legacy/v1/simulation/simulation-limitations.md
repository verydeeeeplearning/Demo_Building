# Simulation Limitations

The Run button shows an educational model. It does not prove that a real physical build is safe in every condition. Current estimates are simplified, modules may be black boxes, and unsupported physics must be shown as unsupported.

Never animate current through invalid, unknown, or unsupported paths.
