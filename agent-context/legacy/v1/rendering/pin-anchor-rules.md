# Pin Anchor Rules

Each supported footprint defines named pin anchors. Controller anchors use board pin names. Module anchors use registry pin names. Breadboard anchors use rail or row coordinates.

Wire endpoints in render plans must map back to validated connection endpoint IDs.
